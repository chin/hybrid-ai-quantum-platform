# Artifacts

This directory stores curated, version-controlled project artifacts.

Use `outputs/` for disposable local run outputs.

Examples of files that belong here:

- architecture diagrams used in the README
- baseline benchmark summaries
- validation reports tied to release milestones
- curated example outputs

Do not commit raw local run output unless it has been reviewed and intentionally promoted.